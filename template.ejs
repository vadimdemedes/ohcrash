Hey, OhCrash just reported an error 🔥! Here are the relevant details:

**Error name:** `<%= name %>`
**Message:** `<%= message %>` <% props.forEach(function (prop) { %>
**<%= prop.name %>:** `<%= prop.value %>` <% }); %>


**Clean stack trace:**

```js
<%- cleanStack %>
```

**Raw error:**

```js
<%- stack %>
```

Best of luck!
